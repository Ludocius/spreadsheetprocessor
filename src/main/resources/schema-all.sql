CREATE TABLE employee  (
                           id IDENTITY PRIMARY KEY,
                           name VARCHAR(30),
                           monthlyPayment VARCHAR(50),
                           active BOOLEAN
);